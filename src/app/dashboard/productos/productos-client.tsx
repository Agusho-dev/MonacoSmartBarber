// Este archivo ya no se usa — los productos se gestionan desde /dashboard/servicios
// Se mantiene vacío para evitar errores de importación en caso de cache.
export function ProductosClient() {
  return null
}
